<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estado presionado -->
    <item android:state_pressed="true" android:drawable="@drawable/eboton_presionado_1" />

    <!-- Estado deshabilitado -->
    <item android:state_enabled="false" android:drawable="@drawable/eboton_no_presionado_1" />

    <!-- Estado normal (por defecto) -->
    <item android:drawable="@drawable/eboton_normal_1" />

</selector>
